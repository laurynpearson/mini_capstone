@products.map do |index|
  { 
    id: index.id,
    name: index.name,
    image_url: index.image_url,
    description: index.description,
    price: index.price,
    discount: index.is_discounted?,
    tax: index.tax,
    total: index.total
  }
end